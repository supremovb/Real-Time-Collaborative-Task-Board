MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/taskboard?retryWrites=true&w=majority
PORT=4000

# Local development
CLIENT_URL=http://localhost:3000

# Production — set this in Railway → Variables (comma-separated for multiple origins)
# CLIENT_URL=https://taskboard-pv.vercel.app
